apiVersion: v1
kind: Service
metadata:
  name: jaeger-ui
  namespace: default
  labels:
    app: jaeger
spec:
  type: LoadBalancer  # or NodePort for local access
  ports:
  - port: 16686
    targetPort: 16686
    name: ui
  selector:
    app: jaeger